name: Accounts - Check

on:
  pull_request:
    branches: [staging]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: install dependencies
        run: |-
          yarn
      - name: run tests
        run: |-
          yarn test
